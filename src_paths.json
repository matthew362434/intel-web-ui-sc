{
    "comment": "This file is used in CI test process. CI will skip tests if no changes found listed in <paths>",
    "paths": [
        "tests",
        "src/web_ui",
        "docs/architecture/schema.graphql"
    ]
}
